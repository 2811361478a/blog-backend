<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.LikeMapper">

    <select id="findByUserIdAndPostId" resultType="Like">
        SELECT * FROM likes WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM likes WHERE post_id = #{postId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO likes (user_id, post_id, create_time)
        VALUES (#{userId}, #{postId}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM likes WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>
</mapper>
